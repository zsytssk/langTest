Boolean = {"true" | "false"};
Null = {"Null"}
Number = { ASCII_DIGIT+ }
String = { !("\"") ~ ANY }
Array = {"[]" |"[" ~ value ~ ("," ~ value)* ~ "]"}

Object = {"{}" | "{" ~ pair ~ ("," ~ pair) "}"}}
value = {Number | String | Boolean | Null | Array | Object}
pair = {String ~ ":" ~ value }

